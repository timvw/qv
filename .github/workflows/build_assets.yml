name: Build assets

on:
  push:

jobs:

  build_assets_and_add_to_release:
    uses: ./.github/workflows/build_assets_and_add_to_release.yml
    with:
      app_name: "qv"
      version: "0.1"
      commitish: ${{ env.GITHUB_SHA }}
      tag_name: "0.1"
      add_to_release: "false"